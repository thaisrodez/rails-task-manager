<h1>My tasks</h1>
<ol>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task.id) %> | <%= link_to 'Delete', task_path(task.id), method: "delete", data: {confirm: "Delete #{task.title}?"} %></li>
<% end %>
</ol>
<%= link_to "Add task", tasks_new_path %>
